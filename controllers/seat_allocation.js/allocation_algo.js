'use strict';

const allocate = (seatMatrix,metricList, preferenceList) => {
    modifiedMetricList = sortMeritListByRank(metricList); 

    for(let studentIndex in metricList){
        // To be Added Further
     }
 

}